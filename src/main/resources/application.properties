# Application properties
spring.application.name=graph-relational-data-app
server.port=8080

# Active profiles
spring.profiles.active=dev

# SQLite database configuration
spring.datasource.url=jdbc:sqlite:./database.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# Neo4j Configuration (for dev profile, these will be overridden by MockNeo4jConfig)
spring.neo4j.uri=bolt://localhost:7687
spring.neo4j.authentication.username=neo4j
spring.neo4j.authentication.password=password

# Logging settings
logging.level.root=INFO
logging.level.com.graphapp=DEBUG
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=ERROR

# Jackson JSON properties
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=UTC

# CORS configuration (handled by WebConfig)
# spring.web.cors.allowed-origins=http://localhost:5000
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
# spring.web.cors.allowed-headers=*
# spring.web.cors.allow-credentials=true